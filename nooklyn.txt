21:45 -

test url with sample listing location: https://nooklyn.com/locations?latitude=x&longitude=x&radius=1

need to figure out how to get this info into jsonapi:
    search_params = {
        longitude: params[:longitude],
        latitude: params[:latitude],
        radius: params[:radius] || 1
    }
    Location.geo_search(search_params).records

curl -d 'session[email]=gallo.j@gmail.com&session[password]=foobar' 'http://localhost:3000/api/v1/sessions'
curl -H 'API-TOKEN: 92feb555b5ac466f9d1e8ba7e83d3642' http://localhost:3000/api/v1/favorites
