github auth

setup
  sudo apt-get install python-pip python-dev nginx libpcre3 libpcre3-dev sqlite3 libsqlite3-dev
  sudo pip install virtualenv uwsgi https://github.com/mitsuhiko/flask/tarball/master PyGithub GitHub-Flask
  cd ~/gitpop
  virtualenv gitpop
  source gitpop/bin/activate
  flask --app=app initdb
  flask --app=app run --host=0.0.0.0
  uwsgi --socket 0.0.0.0:8000 --protocol=http -w wsgi --mount /=app:app
  uwsgi --master --socket 0.0.0.0:8000 --protocol=http -w wsgi --manage-script-name --mount /=app:app
  sudo start gitpop

check version
  python
  import flask
  flask.__version__   => (0.11.dev0)
