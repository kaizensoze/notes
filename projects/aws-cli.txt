git clone https://github.com/jmespath/jmespath.py.git && jmespath
pip install -e . && cd ..

git clone https://github.com/boto/botocore.git && botocore
pip install -e . && cd ..

git clone https://github.com/aws/aws-cli.git
pip install -e .
